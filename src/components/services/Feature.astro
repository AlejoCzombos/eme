---
interface FeatureItem {
  icon: Icon;
  title: string;
  details: string;
  whiteTheme?: boolean;
}
interface Icon {
  src: string;
  alt: string;
}

const { icon, title, details, whiteTheme } = Astro.props as FeatureItem
---
<details id="question" class={`group p-5 pb-4 ${whiteTheme ? "white" : ""} `}>
    <summary class="flex items-center cursor-pointer">
        <img src={icon.src} alt={icon.alt} class={`mr-3 ${whiteTheme ? "size-20 bg-white/80 rounded-full p-3" : "size-14"}`} />
        <img src={whiteTheme ? "/icons/acordeon-arrow-white.svg" : "/icons/acordeon-arrow.svg"} alt="flecha apertura" class="size-10 transition-all group-open:rotate-180 duration-300 ease-in-out" />
        <span class={`font-semibold flex-grow text-2xl ${whiteTheme ? "text-white" : "text-[#505050]"}`}>{title}</span>
    </summary>
    <p class={`mt-2 text-xl group-open:animate-fadeIn ${whiteTheme ? "text-white" : "text-gray-600"}`}>
        {details}
    </p>
</details>

<style>
#question[open] {
  @apply bg-secondary-100/40;
  transition: all 0.5s ease-in-out;
}
#question.white {
  @apply bg-secondary-900/30;
}
#question:not([open]) {
  background-color: transparent;
  transition: all 0.5s ease-in-out;
}
</style>