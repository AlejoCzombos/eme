---
const { text, line = true, class: className, ...props } = Astro.props as { text: string, line: boolean, class: string };
---

<h2 class={`p-2 text-2xl mb-10 ${line ? "border-b-2 border-primary-500" : ""} ${className}`} {...props} >{text.toLocaleUpperCase()}</h2>