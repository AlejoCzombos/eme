---
const { class : className, grid, gap } = Astro.props;
---

<div class:list={[`grid ${grid ? grid : "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"} ${gap ? gap : "gap-5"}`, className]} >
    <slot/>
</div>